# Critter Quest / 萌兽大冒险：项目交付与面试讲解手册

> 文档定位：这不是一份临时开发备忘，而是 Critter Quest 的完整项目档案。它同时服务于代码交付、后续维护、作品集整理和游戏项目面试。
>
> 内容口径：除“可迭代方向”章节外，所有功能描述均以当前仓库源码为准。面试时应明确区分已经实现的能力与未来规划，不夸大线上数据或商业化结果。

---

## 1. 一句话介绍

Critter Quest 是一款以萌宠收集为主题的轻量级单局消除游戏：玩家先选择本局目标宠物，再从明牌区和多层隐藏牌堆中取牌；目标宠物直接进入草窝，其他宠物进入托盘并通过三张同类牌自动消除。玩家需要在托盘被塞满之前收集 15 只目标宠物。

它把三种体验组合在一局里：

- “寻找目标”的视觉搜索；
- “三张同类”的托盘管理；
- “是否冒险翻隐藏牌”的风险决策。

项目的核心价值不只是把规则跑通，而是围绕首次玩家建立了完整的引导、反馈、容错和跨屏适配闭环。

---

## 2. 项目速览

| 项目 | 当前实现 |
|---|---|
| 中文名 | 萌兽大冒险 |
| 英文名 | Critter Quest |
| 游戏 ID | `critter_quest` |
| 路由 / Payload Key | `/critter_quest` / `critter_quest` |
| 类型 | 萌宠收集 + 托盘三消 + 隐藏牌探索 |
| 单局目标 | 收集 15 只本局目标宠物 |
| 卡牌种类 | 共 13 种；第一关使用前 5 种 |
| 牌区 | 27 张明牌 + 8 列、每列 21 张的隐藏牌堆 |
| 托盘 | 默认 7 格，可在局内直接解锁第 8 格 |
| 道具 | Shuffle、Magnet、Move Up，每局各使用一次 |
| 结算 | 胜利、首次失败后的复活、复活后再次失败 |
| 技术栈 | React、TypeScript、Tailwind 工具类、Web Audio API、Web Speech API |
| 外部依赖 | 未为本游戏新增 npm 依赖 |
| 图片资源 | 22 张图片，统一从 CloudFront CDN 加载 |
| 本地静态资源 | `public/games/critter_quest/` 仅保留 `.gitkeep` |
| 数据持久化 | 当前没有存档或跨会话进度 |
| 广告 / 埋点 | Manifest 保留平台广告契约；游戏内尚未接真实广告和业务埋点 |

---

## 3. 产品定位与设计目标

### 3.1 目标用户与使用场景

这是一款偏休闲、低学习成本、适合移动端碎片时间的游戏。目标玩家不需要理解复杂数值，只需认出动物、点击卡牌并观察托盘即可开始。

典型场景包括：

- 在 Facebook Instant Games 一类即开即玩的环境中进行短时体验；
- 通过可爱的角色美术和即时正反馈吸引非硬核玩家；
- 用一到两分钟完成一段清晰的“选择—收集—消除—通关”体验；
- 通过关卡动物种类增加和托盘压力形成难度递进。

### 3.2 三个核心设计目标

1. **三秒能开始**：首屏不放复杂菜单，直接让玩家选择目标宠物。
2. **每次点击都有解释**：目标牌进入草窝，非目标牌飞入托盘，同类牌聚拢，三张牌高亮后消除。
3. **失败可理解、可挽回**：托盘容量清晰可见，提供额外格、三个一次性道具和一次复活机会。

### 3.3 核心体验支柱

- **可爱感**：13 种动物头像、草窝收集、木质托盘、明亮自然色。
- **可读性**：目标牌使用彩虹高亮，教学猫牌使用琥珀高亮，隐藏牌与明牌视觉差异明确。
- **掌控感**：玩家选择目标、决定抽明牌还是隐藏牌，并在危险时使用道具。
- **节奏感**：飞牌动画、延迟三消、连消音效和目标命中语音把状态变化变成连续反馈。

---

## 4. 玩家完整体验

### 4.1 核心循环

```text
选择目标宠物
    ↓
观察明牌区、隐藏牌堆和托盘
    ↓
点击一张宠物牌
    ├─ 命中目标：直接进入草窝，收集进度 +1
    └─ 非目标：按同类分组插入托盘
                    ↓
              同类达到 3 张？
                ├─ 是：高亮 820ms 后移除三张
                └─ 否：继续占用托盘
    ↓
收集满 15 只目标宠物？
    ├─ 是：胜利，可重玩或进入下一关
    └─ 否：托盘是否已满？
              ├─ 否：继续取牌
              └─ 是：首次可复活，再次则失败
```

### 4.2 开局

- 关卡初始化时没有预设目标，状态为 `choosing`。
- 玩家选择的第一张宠物牌会被移出牌区，并直接计为第 1 只已收集目标。
- 第一关首次进入时，教学会强制选择 puppy；教学完成后重开或进入后续关卡时，玩家可自由选择可见宠物。
- 目标选定后，顶部会同时显示目标头像和 `已收集数量 / 15`。

### 4.3 取牌与目标收集

- 明牌区的所有非空卡牌可直接点击。
- 多层主牌堆中，最前面的牌显示动物头像，后方牌显示为隐藏牌。
- 游戏允许玩家点击指定的隐藏牌，而不是只能从牌堆最前端抽取；这构成“冒险翻未知牌”的差异化规则。
- 如果取到目标宠物，该牌不会占用托盘，而是直接计入草窝和顶部进度。
- 目标命中会触发奖励音、动物欢呼音以及随机英文激励语音。

### 4.4 托盘三消

- 非目标牌进入托盘时，相同动物会自动排列到一起，降低玩家的视觉搜索成本。
- 第三张同类牌进入后不会立刻消失，而是先进入 `pendingClear` 状态。
- 三张牌会放大、发光并出现小星星，约 820ms 后才被移除。
- 延迟清除期间会暂时锁定取牌与道具操作，避免快速连点造成状态竞争。
- 950ms 内连续触发三消会形成连消链：普通三消、二连消以上、五连消以上使用不同的声音反馈。

### 4.5 输赢条件

胜利条件：

- 本关目标宠物收集数量达到 15；
- 立即进入 `won` 状态，停止 BGM，播放完成音效并展示胜利结算图；
- 玩家可选择重玩当前关或进入下一关。

失败条件：

- 默认托盘上限是 7 张；
- 某次非目标取牌没有形成三消，并使托盘达到当前上限时，进入失败流程；
- 第一次达到失败条件时进入 `revive`，复活机会消耗后再次达到上限才进入最终 `failed`。

### 4.6 复活

- 每局有一次复活机会。
- 复活会取出托盘最前面的 3 张牌，并尝试依次放回明牌区的空位。
- 托盘因此释放最多 3 个格子，状态回到 `playing`。
- 当前版本点击复活即生效，没有接激励视频广告；这是明确的集成边界，不应在面试中描述成“已完成广告复活”。

---

## 5. 棋盘与界面分区

| 区域 | 玩家看到的内容 | 规则职责 |
|---|---|---|
| 顶部状态栏 | 返回按钮、关卡、目标数量、目标头像、进度 | 提供退出路径和最重要的局内信息 |
| 主牌堆区 | 8 列重叠卡牌，前牌可见、后牌隐藏 | 提供主要牌量和未知探索压力 |
| 备选明牌区 | 3 × 9，共 27 个位置 | 提供低风险、可规划的取牌来源 |
| 草窝区 | 5 个草窝，每个 3 个位置 | 把 15 个目标拆成 5 组，增强阶段性进度感 |
| 托盘区 | 8 个槽位，第 8 格默认锁定 | 承载非目标牌并制造失败压力 |
| 道具区 | Shuffle、Magnet、Move Up | 提供三种不同的局面修复方式 |
| 结算层 | 胜利、可复活、最终失败三种图片面板 | 收束状态并提供下一步操作 |

返回首页使用 `navigate('/home')`，顶部存在可见返回按钮，满足仓库的回退路径要求。

---

## 6. 关卡与牌池设计

### 6.1 当前关卡配置

| 关卡 | 动物种类 | 每类数量 | 总牌数 | 明牌区 | 主牌堆 | 收集目标 |
|---|---:|---:|---:|---:|---:|---:|
| Level 1 | 5 | 39 | 195 | 27 | 168（8 × 21） | 15 |
| Level 2 | 13 | 15 | 195 | 27 | 168（8 × 21） | 15 |
| Level 3+ | 13 | 15 | 195 | 27 | 168（8 × 21） | 15 |

第一关使用：puppy、kitten、bunny、hamster、mouse。

第二关开始使用全部 13 种：puppy、kitten、bunny、hamster、mouse、panda、koala、fox、bear、frog、monkey、penguin、lion。

### 6.2 难度变化

- 第一关只有 5 种动物，同类牌密度高，更容易自然形成三消。
- 第二关把种类提升到 13 种，每种数量降低到 15，托盘中出现更多不同类型，管理压力明显上升。
- Level 3 及以后目前沿用第二关规则，只改变种子生成的牌序；尚未配置独立障碍或更复杂的难度曲线。

### 6.3 确定性洗牌

关卡牌序不是直接使用 `Math.random()`，而是通过自定义种子随机数和 Fisher–Yates 洗牌生成：

- 种子由关卡号计算；
- 同一关重开会得到相同的初始牌序；
- 不同关卡会得到不同布局；
- 确定性布局便于复现教学、调试卡死局面和回归验证。

需要注意：Shuffle 道具刻意使用当前时间参与种子，因此局内洗牌结果不是固定的。

---

## 7. 第一关教学：从说明文字到可操作状态机

第一关教学不是简单弹窗，而是一套会控制可点击目标、测量元素坐标并根据玩家操作推进的交互状态机。

### 7.1 教学目标

教学依次解决四个问题：

1. 本局要收集什么；
2. 隐藏牌可以点击；
3. 非目标牌如何通过三张同类消除；
4. 目标进度在哪里查看。

### 7.2 完整顺序

1. 显示 `Choose your target pet`。
2. 除指定 puppy 外，其余选择被禁用；手指指向 puppy。
3. 玩家点击后，puppy 从原位置放大飞到屏幕中央。
4. 屏幕解释 `This is your target pet!`，随后卡牌飞入第一个草窝。
5. 状态从 `choosing` 切换为 `playing`，初始收集进度变为 1/15。
6. 进入三步隐藏牌教学，明牌区不可操作。
7. 第一步显示 `You can draw any hidden card to keep making matches.`，后两步只保留手势和高亮。
8. 三个固定隐藏牌位置依次为：第一列第 2 张、第三列第 15 张、第七列第 8 张。
9. 当前确定性牌序下，这三张分别是 kitten、kitten、mouse，因此托盘会形成两张 kitten 加一张 mouse。
10. 隐藏牌教学结束后，主牌堆压暗，备选区只放行一张 kitten。
11. kitten 使用琥珀色高亮，与目标宠物使用的彩虹高亮进行语义区分。
12. 玩家点击 kitten 后，托盘中的三张 kitten 进入三消动画并清除。
13. 三消完成后进入目标收集引导：非目标牌变暗且不可点击，只允许点击当前可见的 puppy。
14. 当屏幕上暂时没有可收集的 puppy 时，强制点击阶段结束。
15. 右上角进度旁展示 `还差多少只目标宠物`，6.5 秒后自动关闭，玩家进入自由游戏。

### 7.3 为什么采用固定序列

完全随机的新手局很容易出现以下问题：

- 三次手势落在相近位置，玩家误以为只有那一列可点；
- 随机牌不能组成教学所需的 kitten 对子；
- 教学目标被洗牌破坏，流程无法稳定复现；
- QA 很难用同样步骤回归。

因此，第一关使用确定性牌序与固定坐标协同，保证教学结果稳定，同时正常关卡逻辑仍然复用同一套取牌和结算函数。

### 7.4 教学的工程实现

关键状态和引用包括：

- `tutorialMotion`：控制目标牌的 source → zoom → nest 三阶段动画；
- `unknownCardGuideActive` / `unknownCardGuideHits`：控制三次隐藏牌点击；
- `reserveCatGuideActive` / `reserveCatGuideResolved`：控制 kitten 三消教学；
- `targetCollectGuideActive`：控制只能点击可见目标牌的阶段；
- `progressGuideActive`：控制右上角剩余目标提示；
- `cardButtonRefs`、`firstNestSlotRef`、`targetProgressRef`：获取真实 DOM 元素；
- `useLayoutEffect` + `getBoundingClientRect()`：在渲染后测量位置；
- `layoutTick`：窗口尺寸变化时重新测量，引导层不会停留在旧坐标。

教学只在组件本次挂载期间执行一次。完整退出并重新进入游戏会重新教学；同一次挂载中重开第一关不会重复强制教学。

---

## 8. 三个道具的规则与设计意图

三个道具每局各可使用一次。代码内部初始状态名为 `locked`，但界面含义实际是“尚未使用、可以点击”；使用后变为 `used` 并显示勾号。

### 8.1 Shuffle

规则：

- 收集明牌区所有现存卡牌；
- 收集 8 列主牌堆最前面的可见卡牌；
- 对这些可见牌进行洗牌并放回原位置；
- 不改变空位、隐藏牌顺序、托盘和目标进度。

设计意图：在保留棋盘结构的同时改变眼前可选项，降低直接“改写整副牌”带来的不可控影响。

### 8.2 Magnet

规则：

- 忽略目标宠物，只统计托盘里的非目标牌；
- 优先选择数量最多的同类组；数量相同时选择更早进入托盘的组；
- 自动补足到 3 张，并触发正常的延迟三消；
- 如果托盘没有可处理的牌，不消耗道具，只显示 `No trio to complete`。

设计意图：精准处理最接近三消的牌组，是对托盘空间最直接的修复。

### 8.3 Move Up

规则：

- 取出托盘最后一张牌；
- 放入明牌区第一个空位；
- 如果托盘为空或明牌区没有空位，不消耗道具，只显示 `No space to move up`。

设计意图：把即时托盘压力转换成未来仍需处理的棋盘压力，属于“延期处理”而不是免费消除。

### 8.4 额外托盘格

- 第 8 格默认锁定，正常容量为 7；
- 点击带加号的格子后立即解锁，容量变为 8；
- 当前没有广告门槛或货币消耗；UI 变量中仍保留了早期 `isAdSlot` 命名，但实际逻辑是直接解锁。

---

## 9. 技术架构

### 9.1 文件边界

```text
src/games/critter_quest/
├── manifest.ts   # 路由、标题、首页卡片、广告占位与懒加载入口
├── index.tsx     # 规则、状态、算法、交互组件、教学和页面渲染
├── audio.ts      # Web Audio 音效与 BGM
└── CRITTER_QUEST_PROJECT_GUIDE.md    # 本文档

public/games/critter_quest/
└── .gitkeep      # 资源已迁到 CDN，仅保留目录占位
```

该模块自包含，不修改 `App.tsx`、`Home.tsx`、`registry.ts` 或共享 Service。Registry 会通过 manifest 自动发现游戏，`component: () => import('./index')` 保持懒加载和代码分割。

### 9.2 主要类型

| 类型 | 作用 |
|---|---|
| `CardId` | 约束 13 种合法动物 ID |
| `LevelConfig` | 描述关卡目标、牌区规模和牌堆结构 |
| `PlayState` | 描述一帧可玩的完整状态 |
| `GameState` | 在 `PlayState` 基础上增加历史快照 |
| `GameStatus` | `choosing`、`playing`、`won`、`revive`、`failed` |
| `PowerupId` | `shuffle`、`magnet`、`moveUp` |
| `TutorialMotion` | 描述目标牌在三个动画阶段的几何位置 |
| `CardMoveMotion` | 描述普通牌从源位置飞到托盘槽的几何位置 |

### 9.3 单一状态源

`GameState` 是规则层的单一事实来源，包含：

- 当前关卡和目标宠物；
- 明牌区、8 列牌堆和预留 stock；
- 托盘与待清除牌型；
- 收集进度、额外格、复活和道具状态；
- 游戏状态、提示消息、隐藏牌点击数和开始时间；
- 最多 18 个历史快照。

React 的局部 UI 状态只保存动画与教学覆盖层，例如 DOM 坐标、当前手势步骤和卡牌飞行动画。这样规则状态与展示状态不会互相污染。

历史快照当前会在普通取牌和有效道具操作时记录，但没有对玩家开放 Undo 按钮。它属于已存在的技术基础，不应描述为已交付的撤销功能。

### 9.4 状态机

```text
choosing
   │ 选择第一张目标牌
   ▼
playing ──────────────── 收集满 15 ───────────────▶ won
   │                                                  │
   │ 托盘首次满                                       ├─ 重玩当前关
   ▼                                                  └─ 下一关
revive
   ├─ 重玩当前关
   └─ 使用一次复活 ─────────▶ playing
                                  │
                                  │ 托盘再次满
                                  ▼
                                failed
                                  └─ 重玩当前关
```

状态转换集中在纯函数或明确的 handler 中：

- `createInitialState`：创建关卡状态；
- `finalizeTutorialSelection`：完成第一关目标选择动画；
- `settleCard`：处理目标收集、托盘插入、待消除和失败判断；
- `enterFailure`：根据复活状态进入 `revive` 或 `failed`；
- `completeRevive`：释放托盘空间并恢复游戏；
- `restartLevel` / `startNextLevel`：重新创建关卡。

### 9.5 规则函数尽量纯化

洗牌、计数、托盘分组、目标结算、失败判断和复活等逻辑主要写成纯函数。React handler 负责读取点击来源、播放反馈和提交新状态。

好处是：

- 规则更容易单独推演；
- 不依赖 DOM 的部分可进一步抽成单元测试；
- 教学点击与普通点击最终能落到相同的结算逻辑；
- 状态变化比直接修改多个 `useState` 更可追踪。

---

## 10. 几个关键实现难点

### 10.1 确保三消反馈可见，同时避免竞态

如果第三张牌加入后立即从数组删除，玩家只会看到托盘闪一下，无法理解自己完成了什么。当前方案分两步：

1. 把第三张牌放入托盘并设置 `pendingClear`；
2. 根据牌型找到前三个匹配索引，给它们添加放大、星光和高亮；
3. 暂停取牌与道具操作；
4. 820ms 后统一删除三张牌并清空 `pendingClear`。

这同时解决了反馈可见性和快速连点导致重复结算的问题。

### 10.2 卡牌从任意位置准确飞到托盘

响应式页面中不能把起点和终点写成固定像素。实现方式是：

- 点击时测量源按钮相对游戏根节点的位置；
- 根据同类分组规则提前计算目标托盘索引；
- 下一次布局完成后测量对应托盘槽；
- 用一个浮动 `TrayCard` 在两个矩形之间过渡；
- 动画期间隐藏目标槽的静态牌并锁定新点击；
- 330ms 后释放动画状态。

这个方案让明牌区和不同深度牌堆都能复用同一个飞牌系统，也能在不同屏幕宽度下保持正确终点。

### 10.3 让教学既稳定又不复制玩法逻辑

教学需要强制路径，但如果为教学另写一套结算，很容易和正式规则分叉。当前做法是：

- 教学层只决定“此刻允许点击谁”；
- 卡牌选择仍调用正常的 `submitBoardCard` 或 `submitStackCard`；
- 正式结算仍由 `settleCard` 处理；
- 只有首次目标选择的展示动画由 `finalizeTutorialSelection` 单独收束。

因此，教学 kitten 触发的三消与玩家自由游戏中的三消是同一套机制。

### 10.4 让音频在浏览器限制下可靠启动

现代浏览器禁止页面无手势自动播放声音。音频模块不会在 import 时直接创建并播放，而是在玩家第一次点击时调用 `unlock()`：

- 延迟创建或恢复 `AudioContext`；
- 再启动 BGM 循环；
- 所有声音通过统一 master gain 输出；
- 组件卸载时停止 BGM 定时器。

这既符合浏览器自动播放策略，也避免模块加载时产生全局副作用。

### 10.5 在一屏内兼容移动端和桌面端

页面以 `100svh` 为高度上限，避免移动浏览器地址栏造成传统 `100vh` 抖动；主容器最大宽度为 540px，在桌面端居中呈现，在移动端占据主要宽度。

卡牌、文字、按钮使用 `clamp()`，主棋盘使用 flex 吸收剩余高度，备选区限制为 `17svh–19svh`。引导层会在 resize 后重新测量目标，避免手指和高亮错位。

---

## 11. 视觉与反馈系统

### 11.1 视觉语言

- 背景：绿色自然环境与低透明度涂鸦图；
- 卡牌：暖白卡面、木色边框，突出动物头像；
- 主牌堆：米黄色桌面，隐藏牌使用统一背面；
- 托盘：深木色槽位，强化“容量有限”的容器感；
- 草窝：每组三只目标宠物，视觉上对应三消的“三”；
- 目标高亮：旋转彩虹光圈；
- 教学三消高亮：琥珀色光圈，避免和目标语义混淆。

### 11.2 动画层级

- 按钮按下缩放；
- 非目标牌飞向托盘；
- 首个目标牌放大展示后飞向草窝；
- 待三消牌放大、发光、冒星星；
- 教学手势弹跳；
- 提示箭头指向顶部目标进度。

### 11.3 声音设计

没有引入外部 mp3，`audio.ts` 使用 Web Audio API 的 oscillator、gain、filter 和动态 noise buffer 合成声音。

当前包含：

- UI 点击；
- 选牌与飞牌；
- 普通三消、连续三消、五连消以上；
- 开局、通关、失败、复活、奖励和动物欢呼；
- 菜单与游戏两套短循环 BGM。

命中目标宠物时还会调用浏览器 Speech Synthesis，从以下激励语中随机选择：

- `YES! LET'S GO!`
- `WOOHOO! GOT IT!`
- `BOOM! AMAZING!`
- `THAT'S IT! GREAT JOB!`

如果浏览器不存在 Speech Synthesis，游戏会静默跳过语音，不影响规则运行。

### 11.4 基础可访问性

- 核心交互使用原生 `button`；
- 动物图片带英文 `alt`；
- 返回、卡牌、道具、复活和结算按钮带 `aria-label`；
- 动画层设置 `pointer-events-none`，不会截断真实点击。

当前结算页的视觉文字主要来自图片，透明热区通过 `aria-label` 提供操作名称；这仍不等同于完整的无障碍适配。

---

## 12. 图片资源与 CDN

资源根地址：

```text
https://d2vfwj9kj1j1h1.cloudfront.net/game_center/critter_quest/assets/ui/
```

### 12.1 22 张资源

通用与结算：

- `home_cover.jpg`
- `codex-image-1784019720875.png`
- `collect_grass_nest.png`
- `welldown_page.png`
- `oops_page.png`
- `gameover_page.png`

道具：

- `button_shuffle_source.png`
- `button_magnet_source.png`
- `button_move_up_source.png`

动物：

- `animal_puppy.png`
- `animal_kitten.png`
- `animal_bunny.png`
- `animal_hamster.png`
- `animal_mouse.png`
- `animal_panda.png`
- `animal_koala.png`
- `animal_fox.png`
- `animal_bear.png`
- `animal_frog.png`
- `animal_monkey.png`
- `animal_penguin.png`
- `animal_lion.png`

### 12.2 为什么迁移到 CDN

- 避免 22 张图片进入游戏构建包；
- 让浏览器和 CDN 独立缓存资源；
- 保持游戏目录轻量，便于主项目集成；
- 首页封面和游戏内部图片使用同一发布地址。

不要把这些图片重新放回 `public/games/critter_quest/assets/ui/`。当前 `public/games/critter_quest/` 只保留 `.gitkeep` 是预期状态。

---

## 13. 健壮性与边界处理

当前代码主动处理了以下情况：

- 没有目标时不允许进入正常结算；
- 卡牌飞行动画存在时忽略新的取牌请求；
- 三消待清除时禁止继续取牌和使用道具；
- 教学阶段只放行当前指定目标；
- resize 后重新计算手势、高亮和飞牌坐标；
- Magnet 没有可处理组时不消耗；
- Move Up 没有空位或托盘为空时不消耗；
- 复活状态与复活次数双重检查，避免重复使用；
- 音频 API 不存在或尚未解锁时静默降级；
- BGM 在选择、游戏和结算状态间切换，并在卸载时清理计时器；
- 历史快照限制为 18 份，避免无限增长。

---

## 14. 当前交付状态

### 14.1 Manifest 契约

`manifest.ts` 当前满足：

- `id`、文件夹名、route 和 payloadKey 一致；
- `adType` 保留平台默认 `quiz`；
- iOS / Android 广告位仍为集成占位符；
- 英文与中文标题齐全；
- 首页封面使用 CDN；
- `component` 保持 `() => import('./index')` 懒加载。

### 14.2 已实现清单

- [x] 目标宠物选择和第一张计数
- [x] 27 张明牌区和 8 × 21 隐藏牌堆
- [x] 可点击隐藏牌规则
- [x] 目标牌直接收集
- [x] 非目标牌同类分组入托盘
- [x] 三消延迟高亮与清除
- [x] 连消声音反馈
- [x] 默认 7 格与第 8 格解锁
- [x] Shuffle / Magnet / Move Up
- [x] 胜利 / 复活 / 最终失败
- [x] 第一关完整交互教学
- [x] 卡牌飞行动画与目标飞入草窝动画
- [x] Web Audio BGM 和音效
- [x] 目标命中 Speech Synthesis 激励语音
- [x] 移动端和桌面端适配
- [x] 返回首页路径
- [x] CloudFront CDN 资源切换
- [x] 无新增 npm 依赖

### 14.3 最近验证记录

本次文档更新时的验证（2026-08-11）：

- `npm run build` 通过，Vite 成功转换 123 个模块；
- `git diff --check -- src/games/critter_quest/CRITTER_QUEST_PROJECT_GUIDE.md` 通过；
- 本次只修改 `src/games/critter_quest/CRITTER_QUEST_PROJECT_GUIDE.md`，没有触碰工作区内其他项目的已有改动。

此前的页面与资源交付记录显示：

- 22 张图片已切换到 CloudFront；
- 本地页面验证时没有失效图片或资源加载错误；
- `public/games/critter_quest/` 只保留 `.gitkeep`；
- 没有新增 npm 依赖。

每次新的代码改动后仍应重新执行构建和实际页面回归，不能把历史验证当作永久保证。

---

## 15. 当前边界、技术债与诚实口径

这一节很重要。面试时主动讲清边界，比把 Demo 描述成完整商业产品更可信。

### 15.1 当前没有的能力

- **没有跨会话存档**：刷新页面会重新开始第一关。
- **没有业务埋点**：尚未调用 LogService 或 useSessionTracking。
- **没有真实广告调用**：复活和第 8 格直接生效，虽然 manifest 保留 `quiz` 广告契约。
- **没有游戏内多语言系统**：manifest 有中英文标题，但玩法文案当前为英文硬编码。
- **没有独立的第三关设计**：Level 3+ 复用第二关规模和规则，只改变牌序。
- **没有自动化规则测试**：确定性牌序提高了可回归性，但当前仓库未见 Critter Quest 专属测试文件。
- **没有对外开放 Undo**：状态历史已经记录，但没有撤销按钮。

### 15.2 值得后续整理的代码

- `index.tsx` 同时承载规则、页面和组件，已经达到适合拆分的规模；
- 存在尚未进入当前渲染路径的辅助函数与旧版结算组件，可在不影响集成边界的前提下清理；
- `SkillState` 的 `locked` 命名与实际“可用”含义不一致，建议改为 `available`；
- `isAdSlot` 是早期商业化语义遗留，当前行为是直接解锁；
- `stock` 和部分关卡生成辅助函数为扩展预留，当前关卡没有使用额外 stock。

### 15.3 面试中的推荐说法

可以说：

> 我完成了一个可玩的、具有完整新手引导和失败恢复闭环的游戏原型，并保留了平台 manifest 和广告位契约。当前 Demo 中广告、埋点和持久化还没有接入，我已经把它们作为上线前的明确工作项，而不是把原型能力说成线上能力。

不要说：

> 游戏已经接入广告并有成熟留存数据。

当前仓库没有证据支持后一种说法。

---

## 16. 面试讲解模板

### 16.1 30 秒版本

> Critter Quest 是我做的一款萌宠收集加托盘三消游戏。玩家先选择目标宠物，目标牌直接进入草窝，其他牌进入容量有限的托盘，三张同类自动消除；托盘满则失败。项目的重点是把规则、反馈和教学做成完整闭环：第一关有可操作的状态机教学，卡牌会按真实 DOM 坐标飞入托盘，三消有延迟高亮和连消音效，还提供三个一次性道具及一次复活。技术上使用 React 和 TypeScript，没有新增依赖，图片走 CDN，声音由 Web Audio 动态合成。

### 16.2 两分钟版本

> 这个项目叫 Critter Quest，中文名是萌兽大冒险。我把它定位成移动端碎片时间的轻量休闲游戏。核心循环很简单：先选择一只目标宠物，然后从 27 张明牌和 8 列隐藏牌中取牌。目标牌直接计入 15 只收集进度，非目标牌进入托盘，同类达到三张后消除；如果托盘先满，玩家会失败。
>
> 我主要解决了三个问题。第一是新玩家理解成本。我没有只放规则弹窗，而是做了分阶段的交互教学：强制选择 puppy，看它放大并飞入草窝，再引导玩家点击三个不同位置的隐藏牌，制造两张 kitten，最后点明牌 kitten 完成真实三消。教学只控制可点击对象，底层仍复用正式玩法结算，所以不容易出现教学和游戏规则分叉。
>
> 第二是交互反馈和状态安全。非目标牌会根据元素实际位置飞进正确托盘格，第三张牌进入后先高亮 820ms 再清除。这个期间我用 `pendingClear` 和动画锁阻止重复点击，避免 React 状态在快速操作下产生竞态。
>
> 第三是轻量化交付。图片统一放在 CloudFront，声音不引入 mp3 和第三方库，而是用 Web Audio 合成；整个游戏只存在自己的目录里，通过 manifest 自动注册。当前版本是完整可玩的原型，但真实广告、埋点、存档和多语言仍是上线前工作，我会明确区分已经实现和计划能力。

### 16.3 五分钟深挖结构

按以下顺序讲，最容易让面试官跟上：

1. **产品目标**：移动端、低门槛、短局、萌宠题材。
2. **核心规则**：目标直收，非目标进托盘，三张消除，托盘满失败。
3. **差异化**：隐藏牌可主动探索，不只是传统顶牌逐层解锁。
4. **教学设计**：稳定的固定牌序 + 真实规则复用。
5. **技术难点**：状态机、延迟三消、DOM 测量飞牌、浏览器音频解锁。
6. **容错设计**：第 8 格、三个道具、一次复活。
7. **工程约束**：游戏模块自包含、无新依赖、CDN 资源、manifest 懒加载。
8. **诚实复盘**：缺少埋点、存档、真实广告、多语言和自动化测试。

---

## 17. 三个可重点展开的 STAR 故事

### 17.1 故事一：把随机教学改成稳定可回归的教学

**Situation**

隐藏牌教学最初如果依赖随机位置和随机牌型，玩家可能连续点击同一附近区域，也不一定能自然得到后续 kitten 三消所需的牌。

**Task**

既要保证每次首次体验都能走通同一条学习路径，又不能为教学复制一套假的游戏规则。

**Action**

- 为关卡牌序建立确定性随机数和洗牌；
- 第一关固定三个不同列、不同深度的隐藏牌目标；
- 验证当前牌序下前两张是 kitten，确保形成对子；
- 第三步后只放行备选区一张 kitten；
- 所有点击仍走正式的提交和 `settleCard` 逻辑；
- 用不同颜色区分目标收集与三消教学。

**Result**

教学路径可稳定复现，玩家能在一次流程里理解目标、隐藏牌和三消，QA 也可以用固定步骤回归。

### 17.2 故事二：解决快速点击下的三消竞态

**Situation**

第三张牌入托盘后，如果同步删除，反馈不可见；如果只加动画而不锁操作，玩家可能在计时器结束前继续点击，造成托盘索引变化或重复结算。

**Task**

让玩家看清三消，同时保证异步动画期间状态一致。

**Action**

- 把规则拆成“进入待清除”和“计时结束后实际删除”两阶段；
- 用 `pendingClear` 标识唯一待处理牌型；
- 由当前托盘实时计算前三个匹配索引；
- 统一禁用取牌和道具；
- 820ms 后用函数式 `setState` 基于最新状态删除卡牌；
- 使用 950ms 时间窗统计连消并分层播放反馈。

**Result**

三消过程清晰可见，快速点击不会破坏托盘数据，反馈节奏也从单次消除扩展为连续消除。

### 17.3 故事三：实现跨尺寸准确飞牌

**Situation**

卡牌可能来自明牌区或任意深度、任意列的牌堆，托盘位置又会受屏幕宽度和同类分组影响，固定坐标无法适配。

**Task**

让每张非目标牌都从真实点击位置飞到正确托盘槽，并兼容移动端和桌面端。

**Action**

- 用 ref 保存所有卡牌按钮和托盘槽；
- 点击时先计算同类分组后的插入索引；
- 把源元素和目标槽的 `DOMRect` 转成相对根节点坐标；
- 单独渲染浮动卡牌，用 CSS transition 插值；
- 目标静态牌暂时隐藏，避免出现双影；
- resize 时更新布局测量版本。

**Result**

同一套动画覆盖两种牌源，并在窄屏、宽屏和布局变化后仍能落到正确槽位。

---

## 18. 高频追问与推荐回答

### Q1：为什么目标牌不进入托盘？

> 我希望一局里同时存在“正向收集”和“负向容量压力”。目标牌直接进入草窝，相当于即时奖励；非目标牌进入托盘，相当于待处理成本。两条路径在一次点击后就有明显差异，玩家不需要读长说明也能理解目标。

### Q2：为什么三消要延迟 820ms？

> 如果立即删除，玩家看不到第三张牌完成组合。延迟让入槽、聚拢、高亮、消失形成可读的因果链。为了避免延迟期间的竞态，我把交互一起锁定，并在计时器结束时基于最新状态清除。

### Q3：为什么用确定性牌序？会不会降低重玩性？

> 第一关优先保证教学稳定和问题可复现，所以同关重开使用相同布局。它确实会降低同一关的随机变化，正式产品可以把教学关保留固定种子，普通关卡改成服务端种子或每日种子，在可复现和重玩性之间取得平衡。

### Q4：为什么不用音频文件？

> 这是一个强调轻量交付的模块。Web Audio 可以用很小的代码生成点击、奖励、失败和短循环 BGM，避免额外资源和依赖，也能统一控制音量。缺点是音乐表现力不如制作好的音频资产，正式版本可保留统一音频接口，再替换为压缩后的素材。

### Q5：为什么所有逻辑目前集中在一个 `index.tsx`？

> 早期阶段为了快速迭代和保持模块自包含，我把规则、交互和页面放在一起。现在文件规模已经说明它到了该拆分的阶段。下一步会把纯规则移到 `utils/gameEngine.ts`，教学拆到 hook，展示组件拆进 `components/`，并为纯函数补测试；拆分仍只发生在游戏目录内。

### Q6：如何避免教学和正式玩法不一致？

> 教学不重写结算，只控制当前可点击目标。点击后仍进入正式的 board/stack handler 和 `settleCard`，因此教学里的 kitten 三消和自由游戏里的三消是同一个实现。

### Q7：如果要上线，你下一步先做什么？

> 第一优先级是补关键事件埋点和规则测试，因为没有数据就无法验证教学和难度；第二是接广告失败降级并明确复活/额外格的商业化路径；第三是存档与多语言；最后再扩展关卡障碍和长期成长。顺序上先保证可观测、可回归，再扩大内容。

### Q8：你会关注哪些指标？

当前没有真实数据，可以讲“计划监控”，不能讲成已有结果：

- 教学开始率、每一步完成率和教学退出点；
- 第一次取牌、第一次三消、第一次目标命中的耗时；
- 各关完成率、平均局长、托盘满发生率；
- 三个道具的展示率、使用率和使用后通关率；
- 复活展示率、选择率和复活后完成率；
- 第 8 格解锁前后的失败率；
- 图片、音频和语音能力的失败或降级比例。

### Q9：怎样验证关卡不是死局？

> 当前用确定性布局保证教学路径可复现，但还没有完整求解器。下一步我会把规则纯函数化后做模拟：随机或启发式执行大量局面，统计无解率；生成牌序时增加目标可达性和前 N 步可三消约束；对发布种子做固定回归。这里我会明确说这是下一步，而不是声称现在已经有求解器。

### Q10：广告接入要注意什么？

> `manifest` 已保留平台 `quiz` 广告类型和占位广告位。真正接入时，我会让复活和第 8 格在广告成功回调后生效，同时处理无填充、加载失败、用户关闭和重复点击；本地 AdService 是 no-op，所以必须保留无广告时可继续或明确失败的产品策略。

---

## 19. 可迭代方向

### P0：上线前必须补齐

- 为选目标、教学步骤、三消、道具、复活、输赢和退出增加埋点；
- 为 `buildDeck`、`settleCard`、三个道具、失败和复活补单元测试；
- 把第 8 格和复活接入真实广告成功回调，并处理失败降级；
- 加载 CDN 资源失败时提供占位或重试；
- 将玩家可见文案迁入游戏自己的 i18n 命名空间；
- 明确刷新、退后台和来电中断时的存档策略。

### P1：体验提升

- 增加静音开关、减少动态效果选项和完整键盘焦点样式；
- 为连消增加文字或倍率反馈，不只依赖声音；
- 增加托盘临界容量预警；
- 让道具按钮解释作用，降低图标猜测成本；
- 把首次教学完成状态持久化，避免跨会话重复；
- 增加资源预加载，避免首次出现结算图时闪烁。

### P2：内容与长期性

- 为 Level 3+ 配置独立动物池、目标数、托盘限制和障碍；
- 增加每日种子、关卡星级、连续通关奖励；
- 增加宠物图鉴和收集成长；
- 增加可控的牌序生成器或局面求解验证；
- 结合真实数据调整教程长度、牌型密度和复活时机。

---

## 20. 后续维护指南

继续开发前必须阅读仓库根目录 `AGENTS.md`，并遵守以下边界：

- 只修改 `src/games/critter_quest/` 和 `public/games/critter_quest/`；
- 不修改共享 `App.tsx`、`Home.tsx`、`registry.ts`、Service 或全局 i18n；
- 不为方便而扩展 manifest schema；
- 不把懒加载改成同步 import；
- 不新增 npm 依赖，除非开发者明确批准；
- 不在 import 时挂载新的 `window.*` 或 monkeypatch 共享服务。

关键入口：

- Manifest：`src/games/critter_quest/manifest.ts`
- 规则与页面：`src/games/critter_quest/index.tsx`
- 音频：`src/games/critter_quest/audio.ts`
- 本文档：`src/games/critter_quest/CRITTER_QUEST_PROJECT_GUIDE.md`

教学相关搜索词：

- `findTutorialTarget`
- `findUnknownGuideTarget`
- `findReserveCatGuideTarget`
- `tutorialMotion`
- `unknownCardGuideActive`
- `reserveCatGuideActive`
- `targetCollectGuideActive`
- `progressGuideActive`

规则相关搜索词：

- `buildDeck`
- `createInitialState`
- `settleCard`
- `enterFailure`
- `completeRevive`
- `handleSkillClick`
- `submitBoardCard`
- `submitStackCard`

---

## 21. 交付前回归清单

### 构建与边界

- [ ] `npm run build` 通过
- [ ] Browser console 没有 `[registry]` 校验错误
- [ ] `git status` 中与 Critter Quest 有关的改动只在两个允许目录内
- [ ] 没有新增 npm 依赖
- [ ] `manifest.ts` 仍使用动态 import

### 路由与资源

- [ ] `#/home` 能看到 Critter Quest 首页卡片
- [ ] 点击卡片进入 `#/critter_quest`
- [ ] 顶部返回按钮回到 `#/home`
- [ ] 22 张 CDN 图片无 404
- [ ] 图片加载失败不会导致页面白屏

### 第一关教学

- [ ] 只允许点击指定 puppy
- [ ] puppy 放大并飞入草窝
- [ ] 三个隐藏牌目标依次位于不同位置
- [ ] 只有第一步隐藏牌显示说明框
- [ ] 前两次产生 kitten 对子
- [ ] 备选区只放行指定 kitten
- [ ] kitten 三消完成后才进入目标牌强制收集
- [ ] 无可见目标后出现剩余数量提示并自动消失

### 核心玩法

- [ ] 目标牌不进入托盘，进度正确 +1
- [ ] 非目标牌按同类聚拢
- [ ] 第三张牌高亮后延迟清除
- [ ] 动画期间快速点击不会重复结算
- [ ] 默认 7 格满时进入复活流程
- [ ] 第 8 格解锁后容量变为 8
- [ ] 复活移出最多 3 张牌且只能使用一次
- [ ] 再次托盘满进入最终失败
- [ ] 收集 15 只进入胜利
- [ ] 重玩与下一关按钮命中区域正确

### 道具与音频

- [ ] Shuffle 只重排当前可见牌
- [ ] Magnet 正确补足并触发三消
- [ ] Move Up 把托盘末牌放回首个空位
- [ ] 无有效目标时道具不被消耗
- [ ] 第一次用户手势后 BGM 和音效可播放
- [ ] 页面退出后 BGM 定时器停止
- [ ] 不支持 Speech Synthesis 时游戏仍可正常进行

---

## 22. 集成说明

完成最终回归后，由开发者选择仓库既定脚本，不要手写共享项目迁移代码：

```bash
npm run integrate -- critter_quest F:/business/framework-project/boredday/project
```

如果无法直接访问主项目，则运行：

```bash
npm run export-migration
```

Manifest 的 `adType` 当前保留 `quiz`，iOS / Android 广告位仍是占位符。若主项目需要专属广告位，必须在 PR 或集成说明中明确告知集成者。

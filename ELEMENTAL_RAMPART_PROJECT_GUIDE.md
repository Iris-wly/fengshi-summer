# Elemental Rampart / 元素壁垒：项目交付与面试讲解手册

> 文档定位：Elemental Rampart 的完整项目档案，兼顾代码交付、后续维护、作品集整理和游戏项目面试。
>
> 内容口径：除“上线事件漏斗”和“后续迭代”章节外，功能与数值均以当前仓库源码为准。当前版本没有真实运营数据、业务埋点、广告、存档、音频或多语言，不应把规划描述成已上线结果。

---

## 1. 一句话介绍

Elemental Rampart 是一款竖屏实时生存塔防游戏：弩炮持续自动射击，玩家通过拖动改变射击方向，在 25 波敌军和经验宝箱的压力下不断三选一升级，并于 Lv5、Lv10、Lv15 从五元素中构筑最多三种自动技能；最终面对拥有 5 亿生命的深渊巨兽，在它攻破城门前累计造成 5000 万伤害即可锁定胜利。

这个项目的关键不只是“做了一个塔防”，而是搭建了一套完整的浏览器实时游戏系统：

- React 管理 HUD、升级、暂停和结算；
- Canvas 引擎负责逐帧模拟和绘制；
- 25 波配置驱动敌军、飞行单位、宝箱和 Boss；
- 经验卡池形成单局 Build；
- 五元素拥有相互独立的伤害、频率与效果成长；
- 固定逻辑画布、快照节流和 CDN 资产保证移动 WebView 的性能与一致性。

---

## 2. 项目速览

| 项目 | 当前实现 |
|---|---|
| 中文名 | 元素壁垒 |
| 英文名 | Elemental Rampart |
| 游戏 ID | `elemental-rampart` |
| 路由 / Payload Key | `/elemental-rampart` / `elemental-rampart` |
| 类型 | 自动射击 + 手动瞄准 + 25 波生存塔防 + Roguelite 三选一 |
| 核心目标 | 守住 100 HP 城门，Boss 阶段累计造成 5000 万伤害 |
| 逻辑画布 | 360 × 630，CSS contain 缩放，DPR 上限 2 |
| 战斗波次 | 24 个普通波 + 第 25 波 Boss |
| 敌人 | Voidling、Runner、Flyer、Brute、Shellback、Boss |
| 单局规模 | 695 个地面敌人、48 个飞行敌人、24 个宝箱、1 个 Boss |
| 元素 | Fire、Water、Wood、Earth、Metal；单局最多三种 |
| 倍速 | 1.0x / 2.0x / 3.0x 循环 |
| 技术栈 | React、TypeScript、Canvas 2D、CSS |
| 外部依赖 | 未为本游戏新增 npm 依赖 |
| 运行时资源 | 64 个 WebP，统一从 CloudFront CDN 加载 |
| 音频 | 最终版本刻意保持完全无声 |
| 数据持久化 | 当前没有存档或跨会话进度 |
| 广告 / 埋点 | Manifest 保留平台广告契约；游戏内尚未调用广告或业务埋点 |

---

## 3. 产品定位与体验目标

### 3.1 目标用户与场景

游戏面向喜欢短时成长、屏幕清怪和 Build 选择的移动端休闲玩家。它适合 Facebook Instant Games 一类点击即玩的场景：不经过内部菜单，平台卡片进入后立即开战，用前三秒和前五秒教学让玩家理解“自动射击、拖动瞄准”。

### 3.2 核心体验支柱

1. **即时进入战斗**：没有二次 Play 页面，首波在 3 秒后开始。
2. **低操作门槛**：攻击全自动，玩家只负责调整方向和升级选择。
3. **高密度战场**：敌人以紧密军团推进，击退会沿队伍传播压缩。
4. **可见成长**：弩炮从单发成长到多箭、高频、高伤和高穿透，元素技能逐步加入。
5. **构筑差异**：三次元素解锁从剩余元素中随机三选一，单局最多形成三元素组合。
6. **终局压轴**：Boss 不是传统“打空血条”，而是伤害门槛加城门生存的双重考验。

### 3.3 为什么取消游戏内开始页

平台首页已经承担游戏选择和封面展示。进入游戏后再出现一次 Play 按钮会制造重复确认，增加首个有效操作的时间。因此最终版本直接进入战场，用短暂的可操作手势提示替代长篇 HOW TO PLAY 弹层。

---

## 4. 玩家完整体验

### 4.1 核心循环

```text
平台卡片进入游戏
    ↓
3 秒准备时间 + 5 秒拖动瞄准提示
    ↓
弩炮自动射击，玩家调整方向
    ↓
击杀敌人 / 击破宝箱 → 获得 XP
    ↓
达到升级阈值 → 战斗暂停 → 三选一或免费刷新
    ↓
强化弩炮 / 解锁或强化元素
    ↓
清空当前波 → 3 秒波间休息 → 下一波
    ↓
第 25 波 Boss
    ├─ Boss 破门前伤害不足 5000 万 → Defeat
    └─ 达到 5000 万 → Victory Locked → 继续输出 → Boss 破门后 Victory
```

### 4.2 瞄准与自动射击

- 弩炮从战斗开始就持续自动开火，没有手动射击按钮。
- 玩家按下或拖动 Canvas 时改变 `aimAngle`，松手后保留最后方向。
- 可瞄准角度限制在约 `-145°` 到 `-35°`，避免炮口朝城墙后方射击。
- Pointer 坐标会从 CSS 显示尺寸映射回 360 × 630 的逻辑坐标。
- 移动距离小于 6 个逻辑像素被视为 tap；若附近存在敌人或宝箱，会锁定目标标记 2 秒。
- 锁定目标只用于选择与视觉提示，炮弹仍沿玩家当前瞄准方向飞行，不是自动追踪弹。
- 战斗前 5 秒在弩炮上方显示左右箭头、移动圆点和 `DRAG LEFT & RIGHT`；每次新战局都会出现，不读取教学完成记录。

### 4.3 波次推进

- 第一波在战斗开始 3 秒后生成。
- 普通波必须满足：地面生成队列为空、飞行生成队列为空、敌人全部离场、宝箱已打开或逃脱，才算完成。
- 波次清空后休息 3 秒，再进入下一波。
- 玩家升级时引擎暂停；选择卡牌后从原战斗状态继续。
- 页面切到后台会自动暂停并打开暂停界面。

### 4.4 城门与失败

- 城门初始生命为 100。
- 普通敌人和飞行敌人抵达城门会造成一次伤害并离场。
- 城门生命低于 35 且仍大于 0 时，只触发一次紧急防御：3 秒减伤护盾和 3 秒全局冻结。
- 护盾存在期间城门受到的伤害减半。
- 城门生命归零时结束战斗：普通阶段或 Boss 未达目标时为失败；Boss 已锁定成功时为胜利。

---

## 5. 25 波敌军与数值曲线

### 5.1 敌人基础数据

| 敌人 | 基础 HP | 逻辑速度/秒 | 城门伤害 | 击杀 XP | 击退系数 | 角色职责 |
|---|---:|---:|---:|---:|---:|---|
| Voidling | 170 | 41.7 | 4 | 1 | 1.0 | 基础步兵 |
| Runner | 105 | 63.3 | 3 | 1 | 1.2 | 低血高速单位 |
| Flyer | 60 | 50.0 | 4 | 1 | 0.75 | 绕过路线、元素免疫 |
| Brute | 680 | 28.3 | 12 | 3 | 0.55 | 高血重甲前排 |
| Shellback | 425 | 35.0 | 8 | 2 | 0.7 | 弩箭伤害降低至 65% |
| Boss | 500,000,000 | 23.3 | 阶段攻击 | 0 | 0.2 | 终局伤害与生存考验 |

`enemyConfig.ts` 中 Boss 的通用基础 HP 为旧配置占位，实际生成 Boss 时由 `BOSS_MAX_HP = 500,000,000` 覆盖。

### 5.2 六阶段生命倍率

普通地面敌人、飞行敌人和宝箱按波段共用生命倍率：

| 波次 | HP 倍率 |
|---|---:|
| W1–4 | ×1 |
| W5–8 | ×3 |
| W9–12 | ×10 |
| W13–16 | ×35 |
| W17–20 | ×120 |
| W21–24 | ×400 |

它不是每波微增，而是每四波一次明显台阶，让玩家能感受到 Build 是否跟得上阶段强度。

### 5.3 单波规模

| 波次 | 地面敌人 | 飞行敌人 | 宝箱 XP | 本波理论总 XP |
|---:|---:|---:|---:|---:|
| 1 | 8 | 0 | 25 | 33 |
| 4 | 16 | 0 | 40 | 56 |
| 5 | 16 | 1 | 45 | 62 |
| 8 | 23 | 1 | 60 | 88 |
| 12 | 29 | 2 | 80 | 120 |
| 16 | 36 | 2 | 100 | 152 |
| 20 | 40 | 3 | 120 | 179 |
| 24 | 45 | 4 | 140 | 209 |
| 25 | 1 Boss | 0 | 无宝箱 | 0 |

完整 24 波合计：

- 695 个地面敌人；
- 48 个飞行敌人；
- 24 个经验宝箱；
- 加上第 25 波 Boss，共 744 个战斗敌方单位；
- 全部击杀且所有宝箱开启时理论总经验为 2937。

### 5.4 飞行敌人

- 第 5 波开始加入；W5–8 每波 1 只，W9–16 每波 2 只，W17–20 每波 3 只，W21–24 每波 4 只。
- 从屏幕左右边缘生成，直接飞向城门，不走迷宫路线。
- 不参与地面军团编队、洞口净空或宝箱位置计算。
- 只能被普通弩箭伤害；Fire、Water、Wood、Earth、Metal 均不会伤害或控制飞行敌人。
- 普通弩箭的数量、伤害、穿透和击退仍然有效。
- 引擎会在飞行敌人推进超过约 45% 路程时优先显示目标标记，但实际射击仍取决于玩家方向。

飞行敌人让玩家不能只把瞄准固定在迷宫出口，是对“自动射击 + 手动方向”机制的持续校验。

---

## 6. 军团编队、路线与经验宝箱

### 6.1 交叉迷宫路线

路线由 16 个控制点组成。`RouteSystem` 会把折线拐角转成二次贝塞尔曲线，再按约 1.5 个逻辑像素采样成累计距离表。

运行时每个地面单位只保存 `pathDistance`：

- 用二分查找定位距离所属采样段；
- 在线性插值后得到 `x`、`y` 和朝向；
- 碰撞、移动、编队和绘制共享同一距离坐标；
- 不需要每帧重新解整条曲线。

当前路线刻意保留交叉迷宫结构，不应擅自改成简单 S 型路径。

### 6.2 紧密生成与洞口净空

- 地面单位目标生成间距约为 12 个逻辑像素。
- 实际间隔会结合前后单位半径计算，并限制在 0.2–0.5 秒。
- 每次生成前检查洞口附近所有地面敌人和宝箱。
- 如果某单位被击退回出生口，队列每 0.08 秒重试，而不是把新单位堆在同一坐标。

这解决了密集军团游戏中常见的“出生点叠怪、闪烁和碰撞爆炸”问题。

### 6.3 军团收拢与击退传播

- 每帧按 `pathDistance` 从前到后排序地面敌人。
- 后排与前排距离过大时，以固定追赶速度或自身速度的 70% 向前收拢。
- `enforceQueueSpacing` 再保证后排不能穿过前排。
- 普通弩箭的单位击退在 1 秒窗口内最多累计约 40 个逻辑像素，避免多箭把单个敌人无限推回出生点。
- 被击退单位如果压到后方单位，会把重叠量的 62% 继续向后传播，形成军团压缩效果。
- Renderer 使用暗紫连线、轻微路线摇摆和压缩缩放表现队伍整体感，但所有敌人仍有独立 HP 和独立碰撞。

### 6.4 每波一个经验宝箱

- 宝箱插入地面生成队列中点，拥有固定 `formationIndex`。
- 它会根据前后存活单位的位置和速度保持在原队列中，不因减速、定身或击退超车。
- 普通弩箭在碰撞检测时优先检查宝箱。
- 元素技能不对宝箱造成伤害；Water 可减速，Wood 可定身，Earth 可击退和眩晕。
- 当前 HP 直接绘制在宝箱身上，不额外占用中央 HUD。
- 击破后立即获得 XP 并从路线消失；抵达终点则逃脱，不奖励 XP，也不伤害城门。
- 宝箱基础生命 210，使用与本波敌人相同的生命倍率。
- 第 1 波奖励 25 XP，此后每波 +5，第 24 波为 140 XP。

宝箱是风险收益装置：弩箭优先命中它可能降低即时清怪效率，但成功打开能加速下一次升级。

---

## 7. 经验成长与三选一卡池

### 7.1 升级阈值

累计 XP 阈值从 30 开始，每级所需增量逐步增加 10：

```text
30, 70, 120, 180, 250, 330, ... , 2730, 2970, 3220, 3480
```

全清前 24 波的理论 XP 为 2937，因此正常情况下 Boss 前约 Lv18–22，全清并开启全部宝箱时约 Lv22。

### 7.2 升级流程

1. 击杀敌人或打开宝箱使 XP 跨过当前阈值；
2. 等级 +1，引擎进入 `awaitingUpgrade` 并暂停；
3. React 打开升级弹窗；
4. 从当前有效卡池随机展示三张；
5. 玩家可免费、无限次刷新；
6. 只有本轮实际展示过的 choice ID 才能被应用；
7. 选择后清空候选 ID、恢复引擎并继续战斗。

“只允许应用当前候选”可以避免旧 UI、重复点击或伪造 choice ID 修改不应获得的属性。

### 7.3 卡池阶段

| 等级 | 卡池规则 |
|---|---|
| Lv2–Lv4 | 仅从 12 张弩炮属性牌中随机三张 |
| Lv5 | 从尚未拥有的 5 种元素中随机三张，选择第一个元素 |
| Lv6–Lv9 | 弩炮牌 + 已拥有元素的属性牌 |
| Lv10 | 从剩余 4 种元素中随机三张，选择第二个元素 |
| Lv11–Lv14 | 弩炮牌 + 两种已拥有元素属性牌 |
| Lv15 | 剩余 3 种元素全部成为候选，选择第三个元素 |
| Lv16+ | 弩炮牌 + 三种已拥有元素属性牌 |

达到属性上限的牌自动从卡池退出，不会继续出现无效升级。没有城门修复卡。

### 7.4 弩炮属性

| 属性 | Tier 1 | Tier 2 | Tier 3 | 累计上限 |
|---|---:|---:|---:|---:|
| Volley | +1 | +2 | +3 | 额外 +24，总计 25 枚 |
| Fire Rate | +5% | +10% | +15% | +300% |
| Bolt Damage | +50% | +75% | +100% | +2500% |
| Piercing | +1 | +2 | +3 | 额外 +12 个目标 |

基础弩炮：24 伤害、0.5 秒攻击间隔、1 枚弩箭、0 额外穿透。

计算方式：

```text
单箭伤害 = 24 × (1 + Damage Bonus)
攻击间隔 = max(0.11, 0.5 ÷ (1 + Fire Rate Bonus))
每轮数量 = 1 + Volley Bonus
单箭命中次数 = 1 + Piercing Bonus
```

多箭会以 0.025 弧度为间隔形成对称扇形。引擎保留暴击字段和 1.75 倍暴击结算能力，但当前卡池没有暴击升级，实际暴击率保持 0，不应把暴击描述成当前可构筑能力。

### 7.5 元素属性

每个已拥有元素都有 9 张属性牌：

- Frequency：+5% / +10% / +15%，上限 +150%；
- Power：+50% / +75% / +100%，上限 +1000%；
- Effect：+5% / +10% / +15%，上限 +200%。

频率升级会按“升级前剩余时间占原冷却的比例”换算新剩余时间，避免冷却遮罩突然倒退或凭空重置。

### 7.6 随机性的可复现性

升级卡池使用引擎内部固定初始种子的线性同余随机数。相同操作和刷新序列会得到可复现的候选序列，便于调试；正式运营如需更强重玩性，可以换成局种子、每日种子或服务端种子。

---

## 8. 五元素自动技能

### 8.1 总体规则

- 元素在 Lv5、Lv10、Lv15 解锁，单局最多拥有三种。
- 解锁后自动施放，玩家不能手动点击技能。
- 没有存活地面敌人时不会空放，也不会重置冷却。
- 技能栏只显示元素图标和图形化冷却遮罩，不显示名称、AUTO、等级或秒数。
- 满 Frequency `+150%` 时，冷却约为基础的 40%。
- 所有元素排除飞行敌人。

| 元素 | 基础冷却 | 满频率冷却 | 核心定位 |
|---|---:|---:|---|
| Fire | 10s | 4.0s | 全场爆发 + 燃烧 |
| Water | 11s | 4.4s | 全场伤害 + 持续冰霜 + 减速 |
| Wood | 12s | 4.8s | 前线区域束缚 + 持续伤害 |
| Earth | 13s | 5.2s | 全场伤害 + 击退 + 眩晕 + 城门护盾 |
| Metal | 9s | 3.6s | 沿当前方向五重穿透 + 破甲 |

元素基础伤害并非独立常数，而是和当前弩炮 Build 联动：

```text
元素基础伤害 = 当前单箭伤害 × (1 + min(24, 额外箭数) × 0.22) × 技能系数
最终技能伤害 = 元素基础伤害 × (1 + 元素 Power)
```

因此弩炮 Damage 和 Volley 同时能提高元素伤害，避免“武器流升级对元素流完全无效”。

### 8.2 Fire — GLOBAL BURN

- 对施放瞬间所有存活地面敌人造成 `3.8` 系数爆发伤害；
- 附加基础 3 秒燃烧；
- Power 提高爆发伤害；
- Effect 提高燃烧 DPS 和持续时间；
- 施放后新生成的敌人不会补吃本次伤害；
- 受影响单位显示前后双层火焰状态素材。

### 8.3 Water — GLOBAL FROST

- 对当前全部地面敌人造成 `1.4` 系数直接伤害；
- 附加 3.2 秒持续冰霜伤害；
- 减速基础倍率约为 0.62，Effect 越高减速越强、时间越长；
- Power 提高直接伤害和持续冰霜伤害；
- 宝箱只受到减速和冰霜视觉，不承受元素伤害；
- 不再生成局部地面冰霜区。

### 8.4 Wood — FRONT BIND

- 选择路线最靠前的地面敌人为区域中心；
- 对固定前线范围内单位施加束缚和持续藤蔓伤害；
- 普通敌人被定身；Boss 只减速至约 72%，不会完全定身；
- Effect 延长束缚，不扩大真实命中范围；
- 宝箱在区域内会被定身但不受伤害；
- Boss 的藤蔓素材透明度降低，避免遮挡主体。

### 8.5 Earth — GLOBAL QUAKE

- 对当前全部地面敌人造成 `2.8` 系数伤害；
- 全体击退并眩晕，Boss 的眩晕时间减半；
- 宝箱只受到击退和眩晕；
- Effect 提高击退、眩晕和城门护盾时间；
- 城门获得基础 4 秒、最高约 8 秒的 50% 减伤；
- 每个受影响目标播放一次岩石冲击状态动画。

### 8.6 Metal — AIMED PIERCE

- 沿玩家当前瞄准方向发射 5 枚银蓝弹幕；
- 每枚固定最多命中 3 个地面敌人；
- 伤害系数 `1.35`；
- 命中后施加基础 2.5 秒、1.25 倍承伤的破甲；
- Effect 提高破甲倍率和持续时间，不增加弹幕数量、宽度或穿透数；
- Power 提高弹幕伤害；
- 飞行敌人完全免疫 Metal；
- 正式破甲素材、方向光带和弹幕都沿当前瞄准方向表现。

### 8.7 不采用五行相克

当前没有元素克制。五元素差异来自目标范围、伤害结构、控制类型和 Build 方向。如果同时加入属性克制，玩家需要在高密度战斗中识别敌人属性并理解倍率，会显著提高移动端认知负担。

---

## 9. Boss：伤害门槛而非传统击杀

### 9.1 Boss 规则

- 第 25 波只生成 Abyss Behemoth；
- 最大生命为 500,000,000；
- Boss 不会因 HP 归零而死亡；
- 累计伤害达到 50,000,000 时 `bossSuccess = true`，HUD 显示 `VICTORY LOCKED — KEEP FIRING`；
- 达标后 Boss 进入激怒状态，移动速度提升 30%，并短暂眩晕 0.8 秒作为转折反馈；
- Boss 仍继续走到城门，并每 2 秒攻击一次；
- 攻门伤害按每停留 10 秒增加 2 点，从 3 开始，最多 15；
- Boss 最终打破城门时才结算：已达 5000 万为 Victory，否则 Defeat；
- Earth 护盾可延长城门生存时间，从而增加最终 Boss Damage。

### 9.2 Abyss Break 伤害倍率

Boss 累计承伤会逐段提高后续伤害：

| 累计 Boss Damage | 后续倍率 |
|---:|---:|
| 0 | ×1 |
| 50,000 | ×2 |
| 150,000 | ×4 |
| 400,000 | ×8 |
| 1,000,000 | ×16 |
| 2,500,000 | ×32 |

倍率会参与后续实际伤害并写回累计值，形成明显的破甲加速曲线。HUD、Boss 破防特效和结算总伤害共同把这条曲线可视化。

### 9.3 设计意图

传统 Boss 只要求把血条清零，强 Build 很快结束、弱 Build 直接失败。当前规则把 Boss 变成 DPS Benchmark：

- 5000 万是明确的成功门槛；
- 达标后继续输出，保留“我这套 Build 最终能打多少”的比较空间；
- 城门生命和 Earth 护盾仍然有终局价值；
- 结算页可以展示 Total Boss Damage，支持重玩动机。

---

## 10. 技术架构

### 10.1 分层结构

```text
React 页面层
├── index.tsx：ScreenState、组件组合、Retry/Home
├── GameHUD：低频快照展示、倍速、暂停、返回
├── UpgradeModal：三选一与刷新
├── PauseModal：暂停与恢复
└── ResultModal：胜败和 Boss 数据
        │
        │ imperative handle / callbacks / 100ms snapshot
        ▼
Canvas 适配层
└── GameCanvas：生命周期、DPR、Pointer 映射、后台暂停
        │
        ▼
实时引擎层
├── GameEngine：帧循环、波次、碰撞、技能、升级、结算
├── RouteSystem：曲线路径采样与距离查询
├── LegionFormation：编队间距、收拢和渲染摆动
├── Renderer：Canvas 绘制与特效合成
└── BattleAssets：CDN 图片预加载与释放
        │
        ▼
配置层
├── waveConfig / enemyConfig
├── upgradeConfig / skillConfig
├── level1Config
└── battleArtConfig / assetUrls
```

### 10.2 为什么不用 React 每帧渲染战场

敌人、弹幕、状态特效和军团位置每帧都变化。如果把每个对象都做成 React 节点，会产生大量 reconciliation、DOM 写入和布局工作。

当前方案：

- Canvas 每帧绘制高频对象；
- React 只管理 HUD 和 Modal；
- 引擎每 100ms发布一次 `EngineSnapshot`；
- Snapshot 只包含波次、时间、城门、等级、XP、Boss、技能冷却和倍速；
- 敌人数组、弹幕和特效不进入 React state。

这样把“逐帧模拟”和“声明式 UI”放在各自擅长的层里。

### 10.3 React 与引擎通信

向上：

- `onSnapshot`：更新 HUD；
- `onUpgrade`：打开升级弹窗；
- `onResult`：打开胜负结算。

向下：

- `applyUpgrade`；
- `rerollUpgradeChoices`；
- `pause` / `resume`；
- `setSpeed`。

`GameCanvas` 通过 `forwardRef` 和 `useImperativeHandle` 暴露这些命令，避免 React 父组件直接持有引擎内部可变对象。

### 10.4 屏幕状态

React 使用五态页面状态：

```text
playing ↔ paused
playing → upgrade → playing
playing → victory → Retry / Home
playing → defeat  → Retry / Home
```

Retry 通过递增 `sessionId` 强制重建 `GameCanvas` 和 `GameEngine`，确保敌人、计时器、随机状态、技能和资源生命周期全部从零开始，而不是手工清理几十个字段。

---

## 11. 实时引擎、性能与时序安全

### 11.1 帧循环

引擎使用 `requestAnimationFrame`：

1. 计算当前帧与上一帧的时间差；
2. 把 delta 限制在 0–50ms，避免切后台或卡顿后一步穿越整条路线；
3. 根据 1x / 2x / 3x 重复执行更新；
4. 每个显示帧只渲染一次；
5. 结束后停止申请下一帧。

倍速不是单纯改动画速度，而是每帧多执行 2 或 3 次同样的模拟步，因此波次、移动、射击、技能冷却和战斗计时会同步加速。

### 11.2 单帧更新顺序

```text
推进 elapsed / 冷却
→ 自动发射普通弩箭
→ 波次与生成队列
→ 目标提示选择
→ 元素自动施放
→ 弹幕移动和连续碰撞
→ 敌人状态、移动和攻门
→ 宝箱编队移动
→ 特效生命周期
→ 必要时发布 React Snapshot
```

固定顺序让同一帧中的伤害、死亡、XP、升级暂停和后续移动具有可推理的先后关系。

### 11.3 暂停与后台恢复

- Upgrade、Pause 和 Result 都会暂停引擎更新；
- `visibilitychange` 发现页面隐藏时自动暂停；
- 恢复时重置 `lastFrameAt`，不会把后台时间一次性补算；
- 引擎销毁时取消动画帧、清空实体队列并解绑图片回调。

### 11.4 固定逻辑画布

- 游戏逻辑始终使用 360 × 630；
- CSS 以 4:7 比例 contain 到视口，最大宽度 540px；
- 触摸坐标按显示矩形比例映射回逻辑坐标；
- backing store 乘以 DPR 以保证清晰度；
- DPR 限制到 2，避免高像素手机产生过大的 Canvas 和填充成本；
- CSS 使用 `100svh`，降低移动浏览器地址栏造成的高度跳动。

---

## 12. 碰撞、目标与规则细节

### 12.1 连续碰撞检测

弹幕不是只检查“当前点是否进入圆形”，而是计算从上一帧位置到当前帧位置的线段与目标圆的最短距离。这样高速弩箭和 3x 倍速下也不容易穿过小型敌人。

每枚弹幕保存：

- 剩余命中次数 `hitsLeft`；
- 已命中过的目标 ID Set；
- 伤害、击退、暴击和元素效果；
- 是否仍存活。

因此同一弹幕可以穿透多个不同单位，但不会在相邻帧重复伤害同一目标。

### 12.2 目标提示优先级

在没有玩家 tap 锁定时，视觉目标按以下优先级选择：

1. 已推进超过约 45% 的紧急飞行敌人；
2. 当前经验宝箱；
3. 路线最靠前的敌人。

普通目标至少保持约 0.25 秒，避免高密度战斗中标记快速闪烁。

### 12.3 伤害与控制边界

- Shellback 只对普通弩箭减伤 35%，元素伤害不吃这层减免；
- Metal 可以伤害地面敌人和 Boss，但跳过飞行敌人；
- Fire / Water / Earth 作用于施放瞬间全部存活地面敌人；
- Wood 作用于前线区域；
- 元素不会伤害宝箱，但三种控制可以改变宝箱移动；
- Boss 可以受伤害、减速、击退和部分眩晕，但 Wood 不会完全定身；
- 每个敌人保持独立 HP、状态截止时间和碰撞半径。

---

## 13. Renderer 与战场反馈

### 13.1 绘制顺序

Renderer 的主要合成顺序是：

```text
背景与路线
→ 传送门 / 城墙 / 城门护盾
→ 军团连线
→ 地面战斗特效
→ Boss 光环与目标标记
→ 状态特效 Back 层
→ 敌人 Sprite
→ 状态特效 Front 层
→ 敌人血条
→ 宝箱
→ 弹幕
→ 命中 / 暴击 / Metal 特效
→ 弩炮
→ 不受镜头震动影响的全屏元素冲击层
```

敌人按屏幕 y 坐标和路线距离排序，减少交叉路线上的前后遮挡错误。

### 13.2 Sprite 与状态素材

- 轻型地面单位交替使用火系和冰系 Voidling；
- Brute 与 Shellback 共用重甲石像 Sprite，但渲染尺寸和数值不同；
- Flyer 使用 4×2 Rift Bat，横向移动和明显下冲使用不同行；
- Boss 移动使用 4×2 Sprite，到门后切换专属 4 帧攻城动画；
- 宝箱使用 4 帧待机动画；
- Fire、Water、Wood、Earth、Metal 状态素材均为 6 列 × 2 行，第一行绘制在身体后方，第二行绘制在前方；
- Earth 按 14 FPS 单次播放，其他状态在持续时间内循环。

敌人正式 Sprite 未加载完成时不恢复旧圆球怪物。背景、城墙和宝箱仍有有限的程序化降级，避免整个战场空白。

### 13.3 五元素全屏反馈

- Fire：橙红热光、底部与边缘火焰；
- Water：冰蓝封边与左右冰晶裂线；
- Wood：两侧向中央缠绕的藤蔓；
- Earth：最强震动、扩散冲击环和放射裂纹；
- Metal：严格跟随当前瞄准方向的五重银蓝斩光。

反馈窗口约 0.72–0.96 秒，对应 BattleEffect 持续约 0.84–1.02 秒。使用单次幂衰减并限制总震动幅度，即使在 3x 和高频 Build 下也不会长期遮挡或持续闪屏。

### 13.4 当前无音频

最终版本完全无音乐和音效：

- 没有 `AudioContext`；
- 没有音频资源；
- 没有静音按钮或音频状态；
- 不依赖程序化声音。

这是当前产品取舍，不是加载失败。面试时可以解释为：在 Meta WebView 一致性、自动播放限制和包体收敛阶段，先让视觉反馈独立成立；若未来恢复音频，应以独立模块和用户手势解锁方式接入。

---

## 14. HUD、暂停与结算

### 14.1 HUD

顶部单行包含：

- 返回首页；
- 当前波次 / 25；
- 战斗时间；
- 城门数值和血条；
- 1x / 2x / 3x 倍速；
- 暂停。

城门低于 35% 时，状态面板叠加危险裂纹并切换红橙血条。Boss 出现后增加独立 Boss 面板，显示 Boss HP、Break 倍率、累计伤害和 5000 万目标状态。

左侧等级轨道显示 Lv 与 XP；右侧只显示已解锁元素图标和冷却遮罩。

### 14.2 暂停

- 暂停层显示 `BATTLE PAUSED / RESUME / HOME`；
- 背景遮罩和面板使用独立 `<img>` 层；
- 不依赖 `border-image` 或 CSS 背景图来绘制关键暂停内容；
- 这是针对 Facebook WebView 与本地浏览器渲染差异做的兼容性决策。

### 14.3 结算

- 普通失败使用较短的 1120 高度面板，不显示 Boss Damage；
- Boss 失败和胜利使用 1360 高度面板，并显示 Total Boss Damage；
- 结算展示生存时间、波次、等级，Boss 战额外展示总伤害；
- 胜利标题为 `Victory Locked`，失败为 `DEFEAT`；
- Retry 创建全新引擎，Home 返回 `/home`；
- 按钮使用普通态和按压态正式资源，点击时不改变外框尺寸。

### 14.4 基础可访问性

- Canvas 有玩法 `aria-label`；
- HUD 控件使用原生 button 和动态 `aria-label`；
- Modal 使用 `role="dialog"` 与 `aria-modal`；
- 升级卡在视觉上只显示短文案，但完整累计值保留在 `aria-label`；
- 元素冷却通过 `role="status"` 提供剩余秒数；
- CSS 尊重 `prefers-reduced-motion`，把动画和过渡压缩到 0.01ms。

当前仍不是完整的键盘/读屏游戏：Canvas 内的敌人和瞄准无法被逐个聚焦，这是后续无障碍边界。

---

## 15. CDN、Meta WebView 与资源管理

### 15.1 CDN 根地址

```text
https://d2vfwj9kj1j1h1.cloudfront.net/game_center/elemental-rampart/
```

所有运行时地址通过 `assetUrls.ts` 的 `gameAssetUrl()` 统一生成。组件和引擎只传相对路径，不重复硬编码根地址。

### 15.2 资源目录

```text
elemental-rampart/
├── backgrounds/
├── covers/
├── defenses/
├── enemies/
├── hud/
├── pause/
├── result/
├── rewards/
├── skills/
├── status-vfx/
└── upgrade/
```

资源迁移提交记录显示当前 CDN 运行时集合为 64 个 WebP。本地 `public/games/elemental-rampart/` 已整体删除是预期状态，不应重新放回本地兜底，否则会增大 Meta 单游戏包并形成两套路径。

### 15.3 WebView 兼容经验

本地浏览器正常不代表 Meta WebView 一定正常。项目实际遇到的重点是：

- CSS 背景图和九宫格 `border-image` 在不同 WebView 中可能表现不一致；
- 关键暂停遮罩与面板改成直接 `<img>` 层后更可控；
- 正式资源加载完成前不恢复已淘汰的敌人占位画法；
- 本地和 Meta 测试包必须分别检查背景、HUD、升级、暂停和结算。

---

## 16. 关键工程难点

### 16.1 React UI 与高频 Canvas 引擎解耦

**问题**：战场每帧有大量可变对象，但 HUD 和弹窗更适合 React。

**方案**：实体和模拟留在 GameEngine；每 100ms 发布只读 Snapshot 给 React；React 通过 imperative handle 发送少量命令。

**结果**：避免每帧触发 React 渲染，同时保留声明式 UI、可访问性和组件化 Modal。

### 16.2 密集军团不堆叠、不散架

**问题**：不同速度和半径的敌人会互相穿越；击退可能把单位压回洞口，后续生成再次叠坐标。

**方案**：生成净空检查、formationIndex、追赶收拢、最小间距约束、击退压缩传播和宝箱前后锚定共同工作。

**结果**：视觉上是紧密军团，规则上仍是独立单位；控制和击退不会破坏整体队形。

### 16.3 高速弹幕在 3x 下不穿模

**问题**：只检查弹幕当前点会在低帧率或高倍速下跳过目标。

**方案**：使用上一帧到当前帧的线段与圆形目标做距离检测；Set 防止同一弹幕重复命中；`hitsLeft` 支持穿透。

**结果**：普通弩箭和 Metal 都能在高速度下保持稳定碰撞。

### 16.4 五元素既强烈又可辨认

**问题**：元素自动施放且可高频叠加，只靠小范围特效不易发现，全屏持续闪烁又会遮挡战场。

**方案**：每个元素使用不同的空间方向语言、短反馈窗口、单次衰减、震动幅度上限，并把全屏反馈放在最终合成层。

**结果**：1x–3x 下仍能辨认施放类型，同时敌人状态素材、血条和 HUD 不被长期遮挡。

### 16.5 Boss 既要有终局压力又要支持伤害展示

**问题**：直接击杀会截断强 Build 的展示；只要求生存又弱化成长价值。

**方案**：用 5000 万累计伤害锁定胜利，Break 阶段加速后续输出，但等 Boss 真正破门才结算总伤害。

**结果**：同一 Boss 同时承担成功门槛、Build Benchmark 和结算展示三种职责。

---

## 17. 当前交付状态

### 17.1 Manifest 契约

- `id`、文件夹名、route 和 payloadKey 一致；
- `adType` 保留平台默认 `quiz`；
- iOS / Android 广告位仍为占位符；
- manifest 包含中英文标题；
- 首页封面使用 CDN 的 694 × 256 WebP；
- `component` 保持 `() => import('./index')` 动态加载。

### 17.2 已实现清单

- [x] 进入游戏直接开战和 5 秒瞄准提示
- [x] 弩炮自动射击、拖动瞄准、tap 目标提示
- [x] 1x / 2x / 3x 完整模拟倍速
- [x] 24 普通波 + 第 25 波 Boss
- [x] 五类普通敌人与飞行敌人规则
- [x] 交叉迷宫、曲线采样和军团编队
- [x] 每波经验宝箱及编队位置保持
- [x] XP、等级、阶段化三选一卡池和无限刷新
- [x] 五元素解锁、自动冷却、Power / Frequency / Effect
- [x] Boss Break 倍率和 5000 万锁胜
- [x] 连续弹幕碰撞、穿透和击退传播
- [x] 城门危险态、紧急护盾和后台自动暂停
- [x] 正式 Sprite、状态素材和全屏元素反馈
- [x] 普通失败、Boss 失败和胜利结算
- [x] 固定画布、DPR 限制和移动端 contain 适配
- [x] CDN 统一资源路径和 Meta WebView 暂停层兼容
- [x] 返回首页路径
- [x] 无新增 npm 依赖

### 17.3 历史验证记录

本次文档更新时的验证（2026-08-11）：

```text
npx eslint src/games/elemental-rampart --no-cache    PASS
npm run build                                        PASS（Vite 转换 123 个模块）
git diff --check -- ELEMENTAL_RAMPART_PROJECT_GUIDE.md        PASS
```

本次只修改 `src/games/elemental-rampart/ELEMENTAL_RAMPART_PROJECT_GUIDE.md`，没有触碰游戏代码、共享文件或工作区内其他项目的已有改动。

此前交付记录：

```text
npx eslint src/games/elemental-rampart --no-cache    PASS
npm run build                                        PASS
npm run package:fb -- elemental-rampart              PASS
```

最后已知无声版 Meta 测试包：

```text
fb-package-elemental-rampart.zip
大小：4,137,075 B
生成时间：2026-07-31 17:43:06
```

这些是历史记录。任何新的代码改动后都要重新验证，不能把旧 PASS 当作永久保证。

---

## 18. 当前边界、技术债与诚实口径

### 18.1 当前没有的能力

- **没有真实运营数据和事件埋点**；
- **没有广告调用**，manifest 只有平台契约与占位广告位；
- **没有跨会话存档、关卡继续或长期成长**；
- **没有游戏内多语言**，玩家可见文案当前为英文；
- **没有音乐和音效**，这是最终无声版设计；
- **没有玩家主动释放元素技能**，五元素全部自动施放；
- **没有元素克制系统**；
- **没有 Critter Quest 式的交互教学状态机**，这里只有每局 5 秒操作提示；
- **没有本游戏专属自动化测试文件**；
- **没有完整键盘操作和 Canvas 内读屏导航**。

### 18.2 值得继续整理的代码

- `GameEngine.ts` 与 `Renderer.ts` 都超过 1500 行，适合按波次、战斗、技能和特效拆分；
- 暴击、全局冻结和部分效果类型含有扩展或历史语义，应区分正式能力与保留字段；
- 当前固定随机种子便于复现，但会降低相同操作路径的 Build 随机性；
- 关卡数值主要由手工配置，尚无模拟器或自动平衡报告；
- 图片加载没有超时、重试、错误状态或集中可观测性；
- `INITIAL_SNAPSHOT.xpTarget` 的初始展示值为 5，而引擎首次 Snapshot 会改为真实 30，理论上首帧可能短暂不一致。

### 18.3 推荐面试口径

可以说：

> 我完成了一款 25 波可玩的实时 Canvas 塔防原型，包含高密度军团、经验卡池、五元素 Build、Boss 伤害门槛、正式 CDN 资源和 Meta WebView 兼容。当前版本把核心战斗和交付闭环做完整，但广告、埋点、存档、多语言和自动化平衡仍属于上线前工作。

不要说：

> 游戏已经上线运营，并通过数据证明了留存和商业化效果。

当前仓库没有证据支持后一种说法。

---

## 19. 面试讲解模板

### 19.1 30 秒版本

> Elemental Rampart 是我做的一款竖屏实时生存塔防。弩炮自动射击，玩家拖动改变方向，通过击杀和每波宝箱获得经验，进行 Roguelite 三选一，并在 Lv5、10、15 构筑最多三种自动元素技能。技术上我把高频敌人、弹幕和技能模拟放在 Canvas GameEngine 中，React 只接收每 100ms 的快照来显示 HUD 和弹窗；还处理了 3 倍速连续碰撞、密集军团编队、CDN 资源和 Meta WebView 兼容。最终 Boss 采用 5000 万伤害锁胜、继续输出到破门结算的 Benchmark 设计。

### 19.2 两分钟版本

> Elemental Rampart 是一个面向移动端碎片时间的 25 波生存塔防。玩家进入后直接开战，弩炮持续自动射击，操作只需要左右拖动瞄准。普通波里敌人以密集军团沿交叉迷宫推进，每波中间还夹着一个经验宝箱；打开宝箱能加速升级，但弩箭会优先碰撞它，所以存在清怪和成长之间的取舍。
>
> 成长系统分成两层。Lv2 到 Lv4 只强化弩炮，Lv5、10、15 从剩余五元素中三选一，单局最多形成三元素 Build。之后卡池混合弩炮和已拥有元素的 Frequency、Power、Effect，满上限的牌会自动退出。五元素不是换皮：Fire 全场燃烧、Water 全场冰霜、Wood 束缚前线、Earth 全场击退并保护城门、Metal 沿玩家当前方向穿透破甲。
>
> 工程上我没有让 React 每帧渲染几百个对象，而是写了独立 Canvas 引擎，用 requestAnimationFrame 模拟，把 delta 限制在 50ms，并且只每 100ms把 HUD 快照同步给 React。高速弹幕用线段—圆连续碰撞避免 3 倍速穿模；军团用生成净空、最小间距、追赶和击退传播保持队形。
>
> 第 25 波 Boss 也不是简单清空血条。玩家达到 5000 万累计伤害后锁定胜利，但会继续输出到 Boss 破门才结算总伤害，让 Build 既有通关门槛又有成绩展示。当前版本是完整可玩的原型，真实埋点、广告、存档和多语言仍是上线前工作。

### 19.3 五分钟深挖顺序

1. 产品定位：自动射击降低门槛，手动瞄准保留参与感；
2. 核心循环：清怪和宝箱获得 XP，三选一形成 Build；
3. 内容曲线：24 普通波、六段生命倍率、飞行敌人、Boss；
4. 技术架构：React UI + Canvas Engine + 配置层；
5. 性能策略：rAF、delta 上限、DPR 上限、100ms Snapshot；
6. 算法难点：路径采样、军团编队、连续碰撞、击退传播；
7. 五元素：Power 与 Effect 分离，真实规则不只是表现差异；
8. Boss：伤害门槛、Break 倍率、Victory Locked；
9. 发布经验：CDN、Meta WebView、无声版收敛；
10. 复盘边界：数据、测试、存档、广告和多语言。

---

## 20. 四个可重点展开的 STAR 故事

### 20.1 故事一：把战斗帧循环与 React UI 解耦

**Situation**

实时塔防同时存在数十个敌人、弹幕和状态特效。如果全部使用 React state，每帧都会触发大量对象复制和组件协调。

**Task**

既保留 Canvas 的高频性能，又让 HUD、升级、暂停和结算继续使用 React 组件。

**Action**

- 将实体、物理、波次和技能放进独立 `GameEngine`；
- 用 rAF 推进引擎，Canvas 每显示帧绘制一次；
- 设计小型 `EngineSnapshot`，最多每 100ms同步 React；
- 用 callbacks 传升级和结算事件；
- 用 imperative handle 传选择升级、刷新、暂停和倍速命令；
- Retry 通过重新挂载引擎保证完整重置。

**Result**

高频战场不依赖 DOM diff，React 仍能提供结构化 HUD、Modal 和无障碍属性，两层职责清晰。

### 20.2 故事二：解决密集军团生成和击退后的叠怪

**Situation**

不同速度敌人会散开或超车；击退可能把怪物压回洞口，后续生成与旧怪重叠，出现闪烁和不自然堆叠。

**Task**

保持“军团压境”的密度，同时保留每只敌人的独立 HP、速度和控制效果。

**Action**

- 把普通波构造成带 formationIndex 的连续队列；
- 生成前检查敌人和宝箱的洞口净空；
- 后排过远时追赶，过近时用最小间距回推；
- 宝箱锚定原本相邻的前后成员；
- 击退时把重叠量的 62% 传播给后排；
- Renderer 增加连线、摆动和压缩反馈。

**Result**

军团既密集又不会共点，减速、定身、击退和宝箱插队规则仍能稳定共存。

### 20.3 故事三：避免高倍速弹幕穿模

**Situation**

3 倍速下弹幕一帧移动距离增大，只检查当前坐标可能直接从敌人一侧跳到另一侧。

**Task**

在不引入物理引擎依赖的前提下，支持高速弹幕、穿透、宝箱优先和不同半径目标。

**Action**

- 保存弹幕上一帧位置；
- 计算移动线段到目标圆心的最短距离；
- 与目标半径加弹幕半径比较；
- 用 Set 记录已命中 ID；
- 用 hitsLeft 控制穿透次数；
- 先检测宝箱，再检测敌人；Metal 明确跳过 Flyer。

**Result**

普通弩箭和 Metal 在 1x–3x 下共享稳定的连续碰撞逻辑，不需要第三方物理库。

### 20.4 故事四：处理本地正常、Meta WebView 缺图

**Situation**

暂停面板在本地浏览器显示正常，但 CSS 背景或 `border-image` 在 Meta WebView 中可能只剩按钮，关键框体和遮罩缺失。

**Task**

让本地和 Meta 单游戏包得到一致的暂停与结算体验，并控制包体。

**Action**

- 把关键暂停遮罩和面板改为直接 `<img>` 层；
- 所有地址集中到 `gameAssetUrl()`；
- 验证 CDN 后删除本地 64 个 WebP 副本；
- 清理旧开始页、试制军团、宝箱开启动画和废弃地面技能图；
- 保留本地与 Meta 双环境回归清单。

**Result**

资源来源统一、Meta 包体收敛，关键 UI 不再依赖 WebView 差异较大的 CSS 图片机制。

---

## 21. 高频追问与推荐回答

### Q1：为什么弩炮自动射击，而不是点击射击？

> 自动射击把操作从高频点按降成低频方向决策，更适合移动端长波次战斗。玩家仍要处理飞行敌人、宝箱和迷宫不同区域，所以不会完全失去参与感。升级选择则承担更高层的策略深度。

### Q2：为什么不用 React 渲染敌人？

> 敌人、弹幕、状态和特效每帧变化，DOM 方案会把大量时间花在 reconciliation、对象创建和样式更新上。Canvas 引擎可以在一次绘制上下文中完成整帧；React 只消费每 100ms 的 HUD 快照，两者各自负责适合的更新频率。

### Q3：3 倍速如何保证模拟稳定？

> 每个显示帧执行三次同样 delta 的 update，让所有系统同步加速；delta 上限 50ms 防止后台恢复大步跳跃；弹幕使用线段连续碰撞而不是点碰撞，降低高速穿模。正式版本还可以进一步改成固定时间步和累积器。

### Q4：为什么宝箱要插在军团中间？

> 如果宝箱独立放在安全位置，它只是免费奖励。放进队伍中间且弩箭优先碰撞，会让玩家在“先开箱加速成长”和“优先清掉威胁”之间取舍，同时编队约束确保宝箱不会因为控制效果跑到队伍前后。

### Q5：五元素为什么自动施放？

> 屏幕上已经有瞄准、倍速、升级和密集战场信息。再增加五个主动按钮会提升操作和视觉负担。自动施放让元素选择成为 Build 决策，玩家操作仍集中在瞄准。Metal 通过继承当前瞄准方向保留了玩家输入价值。

### Q6：Power 和 Effect 为什么分开？

> 如果所有元素只有伤害升级，Build 最终只是数字变大。拆分后 Fire 可以偏燃烧、Water 偏减速、Wood 偏控制、Earth 偏击退护盾、Metal 偏破甲，让同一个元素也有不同成长方向。

### Q7：为什么 Boss 达标后不立即胜利？

> 5000 万用于明确通关，继续输出用于展示 Build 上限。让 Boss 最终破门再结算，可以保留 Total Boss Damage、Earth 护盾和城门剩余生命的价值，增强 Retry 动机。

### Q8：怎样做数值平衡？

> 当前是配置驱动的手工曲线：每四波提高生命倍率，敌人数量和宝箱经验递增，理论全清让玩家在 Boss 前到 Lv22。下一步我会把引擎规则抽成无渲染模拟，批量跑不同升级策略，观察各波漏怪率、到达等级、元素组合胜率和 Boss 伤害分布，再反推倍率和卡权重。

### Q9：为什么现在没有声音？

> 最终交付阶段优先保证 Meta WebView 一致性、包体和视觉可读性，曾经的程序化音频被完整移除，所以无声是明确版本状态。未来接入时会做独立音频模块、首次手势解锁、静音偏好和后台暂停，不会把音频混入引擎核心。

### Q10：如果上线，最先补什么？

> 先补事件漏斗和无渲染规则测试，让教学、波次、卡池和 Boss 都可观测、可回归；再接存档、资源错误降级和多语言；若商业化需要，再把继续战斗或重试场景接到广告成功回调。先建立数据与稳定性，再扩内容。

---

## 22. 上线事件漏斗与数据分析方案

> 本节是上线方案，不是已实现埋点。当前代码没有调用 LogService。

### 22.1 激活漏斗

```text
battle_view
  → first_aim_input
  → first_enemy_hit
  → first_enemy_kill
  → wave_1_complete
  → first_upgrade_impression
  → first_upgrade_select
```

核心转化：

- 进入战斗 → 首次瞄准：操作提示是否有效；
- 首次瞄准 → 首次命中：玩家是否理解方向与自动射击；
- 首次命中 → 首杀：初始伤害和目标可读性是否合理；
- 首杀 → 第一波完成：首波难度是否过高；
- 升级展示 → 升级选择：卡片是否看得懂、是否有交互阻塞。

### 22.2 成长与内容漏斗

```text
wave_1_complete
  → level_5_reached
  → first_element_unlock
  → wave_10_complete
  → second_element_unlock
  → wave_15_complete
  → third_element_unlock
  → wave_24_complete
  → boss_reached
```

重点分析：

- 玩家主要在哪一波流失；
- 漏箱是否显著推迟 Lv5 / Lv10 / Lv15；
- 三次元素候选和选择分布是否均衡；
- 哪些弩炮/元素组合更容易到达 Boss；
- 1x / 2x / 3x 使用与失败波次的关系。

### 22.3 Boss 与重玩漏斗

```text
boss_reached
  → boss_break_stage_2
  → boss_damage_target_reached
  → battle_victory
  → result_retry / result_home
```

建议同时记录 Boss Damage 分布，而不只看二元胜负：

- 50%、75%、90%、100% 目标区间玩家比例；
- 达标所需时间；
- 达标后额外输出；
- Boss 到门时城门 HP；
- Earth 拥有率、Effect 等级与最终伤害的关系；
- 失败后 Retry 转化率。

### 22.4 推荐事件表

| 事件 | 触发时机 | 核心属性 |
|---|---|---|
| `battle_view` | 引擎创建并开始 | session_id、viewport、dpr、asset_version |
| `first_aim_input` | 首次 pointer move/tap | elapsed、input_type、aim_angle |
| `first_enemy_hit` | 首次弹幕命中 | elapsed、wave、enemy_kind、speed |
| `first_enemy_kill` | 首次击杀 | elapsed、wave、enemy_kind |
| `wave_start` | 每波创建队列 | wave、gate_hp、level、build |
| `wave_complete` | 波清空 | wave、duration、gate_hp、level、chest_result |
| `chest_opened` | 宝箱 HP 归零 | wave、elapsed、xp_reward、level |
| `chest_escaped` | 宝箱到达终点 | wave、remaining_hp、level |
| `upgrade_impression` | 三张卡展示 | level、choice_ids、reroll_count、build |
| `upgrade_reroll` | 点击 Refresh | level、before_choices、reroll_count |
| `upgrade_select` | 选择卡片 | level、choice_id、tier、build_after |
| `element_unlock` | 获得新元素 | level、element、candidate_elements |
| `speed_change` | 切换倍速 | from_speed、to_speed、wave、elapsed |
| `battle_pause` | 手动或后台暂停 | reason、wave、elapsed |
| `boss_break_stage` | 倍率提升 | multiplier、boss_damage、elapsed |
| `boss_target_reached` | 达到 5000 万 | elapsed、gate_hp、level、build |
| `battle_result` | 胜利或失败 | result、wave、elapsed、level、gate_hp、boss_damage、build |
| `result_action` | Retry 或 Home | action、result、boss_damage |
| `asset_load_error` | CDN 图片失败 | asset_path、screen、network_state |

### 22.5 公共属性

每个战斗事件建议统一携带：

- `game_id = elemental-rampart`；
- `session_id` 和稳定的匿名 player id；
- app / game version；
- 当前 wave、level、elapsed、speed、gate_hp；
- 弩炮 count/rate/damage/penetration；
- 已拥有元素及各自 rate/power/effect；
- 设备视口、DPR、平台与网络类型。

Build 属性应使用结构化、可版本化格式，避免把整段 UI 文案当分析字段。

### 22.6 数据判断示例

- `battle_view → first_aim_input` 低：提示不明显或首屏资源遮挡；
- `first_aim_input → first_enemy_hit` 低：坐标映射、瞄准范围或首波生成位置有问题；
- W5 大幅流失：首次 Flyer 与首次元素解锁可能同时造成理解压力；
- 宝箱逃脱率高但玩家仍通关：宝箱收益可能不够重要；
- Refresh 次数高且选择耗时长：卡池质量或文案辨识度不足；
- 某元素解锁率正常但胜率显著低：技能数值或反馈可能误导；
- Boss 大量集中在 4000–4900 万：门槛略高或终局存活时间略短；
- Victory 后 Retry 低：总伤害对比和结算激励不够强。

---

## 23. 后续迭代方向

### P0：上线前必须补齐

- 按第 22 节接入事件漏斗和资源加载错误监控；
- 将 GameEngine 规则拆出无 Canvas 模式并补波次、碰撞、升级、元素和 Boss 测试；
- 为 CDN 图片增加 `onerror`、超时、关键资源重试或降级；
- 把玩家可见文案迁入游戏自己的 i18n 命名空间；
- 明确刷新、切后台、来电和 WebView 回收时的存档策略；
- 在 360×640、390×844、430×932 和 Meta 包中完整回归。

### P1：体验与平衡

- 用模拟器评估每种 Build 的到 Boss 率和 Boss Damage 分布；
- 为升级候选增加权重、防连续重复和保底逻辑；
- 持久化首次瞄准教学完成状态；
- 增加 CDN 预加载进度和关键资源缺失提示；
- 完善键盘瞄准、焦点管理和减少 Canvas 内动态效果；
- 决定是否恢复独立音频模块并提供静音设置。

### P2：内容与长期性

- 增加第二张地图、不同路径和新的飞行/攻城敌人；
- 增加每日种子、挑战词缀和元素组合成就；
- 增加长期解锁，但避免破坏单局 Roguelite 的公平起点；
- 使用真实漏斗调整 W5、W10、W15 和 Boss 的难度台阶；
- 增加分享 Boss Damage 或 Build 摘要的结果卡。

---

## 24. 关键文件与维护边界

```text
src/games/elemental-rampart/
├── manifest.ts
├── index.tsx
├── assetUrls.ts
├── ElementalRampart.css
├── components/
│   ├── GameCanvas.tsx
│   ├── GameHUD.tsx
│   ├── ElementSkillButton.tsx
│   ├── UpgradeModal.tsx
│   ├── PauseModal.tsx
│   └── ResultModal.tsx
├── data/
│   ├── level1Config.ts
│   ├── enemyConfig.ts
│   ├── waveConfig.ts
│   ├── skillConfig.ts
│   ├── upgradeConfig.ts
│   └── battleArtConfig.ts
└── engine/
    ├── GameTypes.ts
    ├── GameEngine.ts
    ├── RouteSystem.ts
    ├── LegionFormation.ts
    ├── Renderer.ts
    └── BattleAssets.ts
```

继续开发前必须遵守根目录 `AGENTS.md`：

- 只修改 `src/games/elemental-rampart/` 和 `public/games/elemental-rampart/`；
- 不修改共享 shell、registry、App、Home、Service 或 package.json；
- 不扩展 manifest schema；
- 不把动态 import 改成同步 import；
- 不新增 npm 依赖，除非开发者明确批准；
- 不恢复已经删除的本地 CDN 资源副本；
- 不自行 commit、push、integrate 或 export-migration。

---

## 25. 交付前回归清单

### 构建与边界

- [ ] `npx eslint src/games/elemental-rampart --no-cache` 通过
- [ ] `npm run build` 通过
- [ ] `npm run package:fb -- elemental-rampart` 通过
- [ ] Browser console 没有 registry 校验错误
- [ ] 本游戏改动只在允许的两个目录
- [ ] 没有新增依赖，manifest 保持动态 import

### 画布与输入

- [ ] 360×640、390×844、430×932 竖屏无溢出
- [ ] DPR 1、2 和高 DPR 设备图像清晰且 backing store 不超过 2x
- [ ] 点击与拖动在缩放后仍指向正确逻辑位置
- [ ] 松手后保留方向，tap 能短暂锁定附近目标
- [ ] 首 5 秒教学可见，5 秒后自动消失
- [ ] 1x / 2x / 3x 循环与战斗计时同步
- [ ] 切后台自动暂停，恢复不补算后台时间

### 波次、敌人和编队

- [ ] 第一波 3 秒后生成，清空后 3 秒进入下一波
- [ ] 洞口被击退单位或宝箱占用时不会重叠生成
- [ ] 军团保持紧密但单位不互相穿过
- [ ] 宝箱位于队伍中部，不因控制而超车
- [ ] 宝箱打开给 XP，逃脱不给 XP 且不伤门
- [ ] W5 起 Flyer 数量符合波段配置
- [ ] Flyer 走直线且只受普通弩箭影响
- [ ] Shellback 普通弩箭减伤正确
- [ ] 所有敌人拥有独立 HP 和碰撞

### 升级与元素

- [ ] Lv2–4 只出现弩炮牌
- [ ] Lv5 / 10 / 15 只出现未拥有元素
- [ ] 其他等级混合有效弩炮牌和已拥有元素属性牌
- [ ] 免费刷新生成新的当前有效候选
- [ ] 达到属性上限后无效牌退出卡池
- [ ] Frequency 升级后冷却比例连续
- [ ] Fire 全场爆发和燃烧正确
- [ ] Water 全场伤害、减速和宝箱控制正确
- [ ] Wood 只作用前线区域，Boss 不被完全定身
- [ ] Earth 全场击退、眩晕和城门护盾正确
- [ ] Metal 严格沿当前瞄准方向，固定五枚、三穿透并施加破甲
- [ ] 五元素始终不影响 Flyer

### Boss 与结算

- [ ] 第 25 波 Boss HP 为 500,000,000
- [ ] Break 在对应累计伤害阈值切换到 ×2/4/8/16/32
- [ ] 50,000,000 时显示 Victory Locked 但不立即结算
- [ ] 达标后 Boss 加速，继续攻门并累计总伤害
- [ ] 未达标破门为 Defeat，达标破门为 Victory
- [ ] 普通失败不显示 Boss Damage
- [ ] Boss 结算显示生存时间、波次、等级和总伤害
- [ ] Retry 完全重置，Home 返回 `/home`

### 视觉、CDN 与 Meta

- [ ] 64 个运行时 WebP 在 CDN 无 404
- [ ] 不出现旧圆球怪、旧开始页或旧地面技能图
- [ ] Sprite 行列、方向镜像和 Boss 攻门动画正确
- [ ] 状态 VFX 的 Back / Front 层正确包裹主体且不遮血条
- [ ] 五元素在 1x / 2x / 3x 下一眼可辨且不持续遮挡
- [ ] HUD 最长波次、时间、城门和 Boss 数据不溢出
- [ ] 城门低血裂纹、紧急护盾和危险血条正确
- [ ] 暂停遮罩、面板、标题和按钮在本地与 Meta 一致
- [ ] 游戏全程无音乐、音效和声音按钮

---

## 26. 集成说明

完成最终回归后，由开发者选择仓库既定脚本，不要手写共享项目迁移代码：

```bash
npm run integrate -- elemental-rampart F:/business/framework-project/boredday/project
```

如果无法直接访问主项目，则运行：

```bash
npm run export-migration
```

Manifest 的 `adType` 当前保留 `quiz`，iOS / Android 广告位仍是占位符。如主项目需要专属广告位或把 Retry 设计成商业化节点，必须在 PR 或集成说明中明确告知集成者。
